var count = 0;

window.onbeforeunload = function (event) {
 
};
function validateReload(e) {
  /** 
  var user = document.getElementById("user");

  if (user == null) {
    if (!location.href.includes("?1")) {
      location.assign(location.href + "?1");
      console.log(location.href);
    }
  }
 

  */
  /*
  if (!location.href.includes("?1")) {
    location.assign(location.href + "?1");
    console.log(location.href);
  }
 */
  //location.reload()
  //history.back()
  //location.reload();
}
